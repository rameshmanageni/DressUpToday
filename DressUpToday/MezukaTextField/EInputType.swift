

import Foundation

public enum EInputType : String {
    case DEFAULT = "Default"   // Default raw value
    case EMAIL = "Email"   // Email raw value
    case PHONE = "Phone" // Phone raw value
    
}

public enum EKeyboardType : String {
    case Default = "default"   // default raw value
    case NumberPad = "numberPad" //numberPad raw value
    case EmailAddress = "emailAddress" // emailAddress raw value
}
